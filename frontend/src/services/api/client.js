// Central API client entrypoint
// Usage: import api from '@api/client'
// Currently re-exports the existing axios instance; later we can swap implementation without changing imports.
export { default } from '../../token/interceptors';
